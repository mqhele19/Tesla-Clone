import React from 'react';
import {View, Text} from 'react-native';

const StyledButton = (props) => {
	return (
	<View>
	<Text>Just a component</Text>
	</View>
	);
	};
	
	export default StyledButton ;